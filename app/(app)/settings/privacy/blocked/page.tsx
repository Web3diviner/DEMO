import { BlockedAccountsScreen } from "@/components/settings/blocked-accounts-screen";

export const metadata = { title: "Blocked accounts" };

export default function BlockedAccountsPage() {
  return <BlockedAccountsScreen />;
}
